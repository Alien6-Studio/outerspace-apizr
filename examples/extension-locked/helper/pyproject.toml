[project]
name = "apizr-locked-helper"
version = "1.0.0"
description = "Local-only locked dependency demonstration; never publish"
requires-python = ">=3.11,<3.15"
license = "GPL-3.0-or-later"
dependencies = ["apizr-locked-leaf==1.0.0"]

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/apizr_locked_helper"]
