[project]
name = "apizr-locked-leaf"
version = "1.0.0"
description = "Local-only locked dependency demonstration; never publish"
requires-python = ">=3.11,<3.15"
license = "GPL-3.0-or-later"
dependencies = []

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/apizr_locked_leaf"]
