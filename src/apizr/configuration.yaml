# Complete pipeline configuration; all sections are optional.
# Defaults to the running interpreter. Uncomment to target Python 3.11.
# python_version: [3, 11]
encoding: utf-8
code_analyzr:
  functions_to_analyze: null
  ignore: null
fast_apizr:
  api_filename: app.py
dockerizr:
  docker_image: debian
  docker_image_tag: slim
  custom_packages: []
  dependencies: []
  server:
    workers: 1
    timeout: 60
    host: 0.0.0.0
    port: 5001
